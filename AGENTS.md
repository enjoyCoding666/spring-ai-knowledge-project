# 项目规范

## 隐私规范

- 仓库中的 SQL 文件只允许记录表结构、扩展、索引、约束和其他 DDL。
- 禁止导出、保存或提交任何真实表数据，包括 `INSERT`、`COPY` 数据转储和数据备份。
- 禁止把来自表数据的待办事项、账号密码、Token、连接串或其他业务隐私写入仓库。
- 非表数据的本地配置凭据只有在用户明确指定为例外时才允许写入配置文件。
- 针对特定本地数据的调整只能在用户明确授权后直接执行，不得写入 SQL 脚本。
- 测试数据和文档示例必须是虚构内容，不得来自本地数据库。
- 提交 SQL 文件前必须检查其不包含表数据。


## 分支选择

- 除非用户明确指定其他分支，否则在该工作区之前指定的分支上进行开发。
- 修改代码前，确认当前分支与工作区指定的开发分支一致。
- 不要仅因为工作区被外部切换，就在另一个分支上继续开发。

## 代码规范

- 非必要，不要修改其他人编写的代码。
- 完成代码后无需编译。
- 尽量不要在 `for` 循环中查询数据库。
- 使用异步处理解耦耗时较长的业务逻辑。
- 不要使用魔法值，应定义常量或枚举。
- 不要在代码行末尾添加注释。
- 方法级注释和行内注释使用中文，日志内容使用英文。
- 打印日志尽量使用debug级别，错误日志使用error级别。
- 请求 DTO 字段可能存在空值语义时，应避免使用 Java 基本数据类型。
- 初始化对象时，优先使用无参构造方法。
- 数据库查询条件和过滤逻辑应放在 SQL 中，不要先查询大量数据再在 Java 中过滤。
- 如果类中已经使用 `@Data` 等 Lombok 注解，不要重复手写 Getter 和 Setter。
- 批量更新和批量删除必须分批处理；批次大小应通过配置项控制，默认值为 1000。
- 建议在方法上方添加简洁的 Javadoc 风格注释，例如：

```java
/**
 * 简要说明。
 */
 
 
 ## sql 规范

- SQL 尽量简单，避免复杂慢 SQL。
- 不要使用三个表及以上的 join。

## 包结构规范

- DTO 和 VO 默认不要放在 `common` 模块中，应优先放在对应服务所属的业务模块中。

## 数据库访问安全
• 测试环境和生产环境数据库连接严格限制为只读。
• 只允许执行数据查询。
• 禁止新增、更新、删除、清空、修改表结构或以其他方式修改数据及数据库对象。

## 分页规范

- 使用项目已有的分页参数。
- 使用项目已有的分页结果类型。

## Skill 使用偏好

- 使用 `clean-code` / Java clean-code 指南保证代码质量。
- Java 代码审查使用 `java-code-review`。
- 调试问题使用 `debugging-strategies`。
- 编写或优化 SQL 时使用 `sql-optimization-patterns`。
- 适用时，单元测试采用 TDD 导向的工作流程。
